package Common;

import java.util.LinkedList;
import java.util.List;
import java.util.UUID;

public class Event {

	private UUID eventID;
	private List<Photo> eventPhotos = new LinkedList<Photo>();;
	private int horizontalPhotosCount = 0;
	private int verticalPhotosCount = 0;;
	
	public Event() {
		eventID = UUID.randomUUID();
	}
	
	public boolean isEmpty() {
		return (eventPhotos.size() == 0); 
	}
	
	public int get
	
	public void addPhotoToEvent(Photo photo) {
		if (photo.isHorizontal()) 
			horizontalPhotosCount++;
		else 
			verticalPhotosCount++;
		
		// two sided referencing
		eventPhotos.add(photo); 
		photo.attachToEvent(this);
	}
}
